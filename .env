# Shared defaults for all environments.
# Override these in .env.development / .env.production / .env.local as needed.
VITE_API_BASE_URL=
VITE_PROD_API_BASE_URL=https://cookingapp-6pj2.onrender.com
VITE_DEV_PROXY_TARGET=http://localhost:8080
VITE_DEV_SERVER_PORT=5173

# Cognito auth settings (fill with your real values per environment)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_REGION=
VITE_COGNITO_USER_POOL_CLIENT_ID=
VITE_COGNITO_BACKEND_TOKEN_USE=access
