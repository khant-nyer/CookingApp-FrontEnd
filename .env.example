# Keep empty in local dev to use Vite proxy (/api -> http://localhost:8080)
# VITE_API_BASE_URL=

# Set this only when calling backend directly or in non-dev environments.
# VITE_API_BASE_URL=http://localhost:8080

# Cognito config (frontend direct login/logout/forgot password)
VITE_COGNITO_USER_POOL_ID=ap-southeast-2_rt542m5n0
VITE_COGNITO_REGION=ap-southeast-2
VITE_COGNITO_USER_POOL_CLIENT_ID=7frnm8fk0j7iv8mqg54fiqd9cp

# Token sent to backend Authorization header: access (default) or id
VITE_COGNITO_BACKEND_TOKEN_USE=access
